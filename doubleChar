public String doubleChar(String str) {
  
  int n=str.length();
 char[] ch=new char[2*n];
  int k=0;
  for(int i=0;i<n;i++)
  {
    ch[k]=str.charAt(i);
    k++;
    ch[k]=str.charAt(i);
    k++;
    
  }
  String s=new String(ch);
  return s;
  
}
