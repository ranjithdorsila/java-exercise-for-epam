public int countCode(String str) {
  int c=0;
  int n=str.length();
  for(int i=0;i<n-3;i++)
  {
    if((str.substring(i,i+4)).startsWith("co") && (str.substring(i,i+4)).endsWith("e"))
    c++;
  }
  return c;
}
